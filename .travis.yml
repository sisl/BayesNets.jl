language: julia
coveralls: true
os:
  - linux
  - osx 
  - windows
julia:
  - 1.0
  - 1
matrix:
  allow_failures:
    - julia: 1.0
notifications:
  email: false
after_success:
  - julia -e 'Pkg.add("Documenter")'
  - julia -e 'cd(Pkg.dir("PACKAGE_NAME")); include(joinpath("docs", "make.jl"))'
